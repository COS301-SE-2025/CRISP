[tool:pytest]
DJANGO_SETTINGS_MODULE = crisp_project.settings
python_files = tests.py test_*.py *_tests.py
python_paths = .
testpaths = tests UserManagement/tests
addopts = 
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
    --cov=UserManagement
    --cov-report=html:coverage/htmlcov
    --cov-report=xml:coverage/coverage.xml
    --cov-report=term-missing
    --cov-fail-under=80